{
  "name": "@eoe/calc-client",
  "version": "1.0.0",
  "private": true,
  "description": "Local runner for @eoe/calc-core — loads data from the backend export API and runs the calculation locally.",
  "license": "UNLICENSED",
  "main": "dist/run.js",
  "bin": {
    "calc-run": "dist/run.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/run.js"
  },
  "dependencies": {
    "@eoe/calc-core": "1.0.0",
    "axios": "^1.9.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "~4.6"
  }
}
